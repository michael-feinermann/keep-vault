<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>Keep Vault Release Verifier</AssemblyName>
    <Product>Keep Vault</Product>
    <AssemblyTitle>Keep Vault Release Verifier</AssemblyTitle>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\KalynaArchiver\KalynaArchiver.csproj" />
    <ProjectReference Include="..\KalynaArchiver.Signing\KalynaArchiver.Signing.csproj" />
  </ItemGroup>

</Project>
